import styled from "styled-components";

const AlertCustom = styled.div`
  border-radius: 30px;
  margin-top: 10px;
  padding: 20px;

  color: var(--darkRed);
  background-color: var(--red);
`;

export { AlertCustom };
