import styled from "styled-components";

const AlertCustom = styled.div`
  margin-top: 10px;
  padding: 20px;
  border-radius: 30px;

  color: var(--darkRed);
  background-color: var(--red);
`;

export { AlertCustom };
